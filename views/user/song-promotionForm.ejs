<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Song Promotion Form</title>
  <link rel="stylesheet" href="/styles/promotion-form.css">
</head>
<style>
    /* promotion-form.css */

body {
  font-family: 'JetBrains Mono', monospace;
  background-color: #f0f4f8;
  margin: 0;
  padding: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  /* height: 100vh; */
}

.promotion-form-container {
    margin-top: 20px;
  background-color: #ffffff;
  border-radius: 10px;
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
  padding: 20px;
  width: 100%;
  max-width: 500px;
}
.mandatory {
    color: red; 
    margin-left: -7px;
}

h2 {
  text-align: center;
  color: #333333;
  margin-bottom: 20px;
  font-size: 1.8rem;
}

.form-group {
  margin-bottom: 15px;
}

.form-group label {
  display: block;
  color: #666666;
  font-size: 1rem;
  margin-bottom: 5px;
}

.form-group input,
.form-group select,
.form-group textarea {
  width: 100%;
  padding: 10px;
  border: 1px solid #cccccc;
  border-radius: 5px;
  font-size: 1rem;
  box-sizing: border-box;
  transition: border-color 0.3s ease;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
  border-color: #007bff;
  outline: none;
}
/* Style for the form-group with a read-only input */
.form-group {
  margin-bottom: 15px;
}

.form-group label {
  display: block;
  color: #666666;
  font-size: 1rem;
  margin-bottom: 5px;
}

.form-group input[readonly] {
  width: 100%;
  padding: 10px;
  border: 1px solid #cccccc;
  border-radius: 5px;
  background-color: #f5f5f5; /* Light gray background to indicate read-only */
  color: #888888; /* Gray text color to indicate it's not editable */
  font-style: italic; /* Italicize the text to further indicate it's not editable */
  box-sizing: border-box;
  cursor: not-allowed; /* Changes cursor to indicate the field is not editable */
}

.form-group input[readonly]:focus {
  border-color: #cccccc; /* No change in border color on focus */
  outline: none; /* Removes the focus outline */
}

textarea {
  resize: vertical;
}

.submit-btn {
  background-color: #007bff;
  color: white;
  font-size: 1.2rem;
  border: none;
  border-radius: 5px;
  padding: 10px 15px;
  width: 100%;
  cursor: pointer;
  transition: background-color 0.3s ease, transform 0.2s ease;
}

.submit-btn:hover {
  background-color: #0056b3;
  transform: translateY(-2px);
}

.submit-btn:active {
  background-color: #003f7f;
  transform: translateY(0);
}

@media (max-width: 500px) {
  .promotion-form-container {
    padding: 15px;
  }

  h2 {
    font-size: 1.5rem;
  }

  .submit-btn {
    font-size: 1rem;
  }
 
}
</style>
<body>

  <div class="promotion-form-container">
    <h2>Promote Your Song</h2>
    <form action="/user/song-promotion/submit" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label for="songTitle">Song Title <span class="mandatory">*</span></label>
        <input type="text" id="songTitle" name="songTitle" required>
      </div>

      <div class="form-group">
        <label for="artistName">Artist Name <span class="mandatory">*</span></label>
        <input type="text" id="artistName" name="artistName" required>
      </div>

      <div class="form-group">
        <label for="albumName">Album Name </label>
        <input type="text" id="albumName" name="albumName" readonly value="not-available ">
      </div>

      <div class="form-group">
        <label for="category">Category </label>
        <select id="category" name="category" required>
          <option value="edm">EDM</option>
          <option value="bollywood">Bollywood</option>
          <option value="circuit">Circuit</option>
          <option value="marathi">Marathi</option>
          <option value="soundcheck">Soundcheck</option>
          <option value="house">House</option>
        </select>
      </div>

      <div class="form-group">
        <label for="releaseDate"> Release Date <span class="mandatory">*</span></label>
        <input type="date" id="releaseDate" name="releaseDate" value="<%= new Date().toISOString().split('T')[0] %>" readonly>
      </div>

      <div class="form-group">
        <label for="songFile">Upload Song  <span class="mandatory">*</span></label>
        <input type="file" id="songFile" name="songFile" accept=".mp3" required>
      </div>

     

      <div class="form-group">
        <label for="description">Description:</label>
        <textarea id="description" name="description" rows="4" placeholder="Tell us more about your song..."></textarea>
      </div>

      <button type="submit" class="submit-btn">Submit for Promotion</button>
    </form>
  </div>

</body>

</html>
